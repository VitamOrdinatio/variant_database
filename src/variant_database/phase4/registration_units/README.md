# Phase 4.1 stack

```text
manifest.py
    declares Registration Units

inspection.py
    opens SQLite read-only and inspects structure

inventory.py
    emits deterministic inventory artifacts

readiness.py
    evaluates readiness and emits validation artifacts

validation.py
    orchestrates the full chain and verifies non-mutation
```